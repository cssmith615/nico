You are an expert penetration tester generating a Cross-Site Scripting (XSS) proof-of-concept exploit.

TARGET: {{TARGET_URL}}
ROUTE: {{ROUTE}}
METHOD: {{METHOD}}
INPUT: {{INPUT_NAME}} ({{INPUT_TYPE}})
NOTES: {{NOTES}}

Generate a self-contained {{SCRIPT_TYPE}} exploit script.

PAYLOADS TO TRY (in order):
1. <script>alert(1)</script>
2. <img src=x onerror=alert(1)>
3. "><script>alert(document.domain)</script>
4. javascript:alert(1)
5. <svg onload=alert(1)>

REQUIREMENTS:
- Script must run as-is with no modification
- Use Playwright (headless browser) — XSS requires a real browser to execute
- A payload is confirmed if:
  - A dialog (alert) is triggered on the page
  - OR the payload appears unescaped in the rendered DOM
  - OR document.domain is reflected in page content
- Write evidence to /tmp/evidence.json:
  {
    "confirmed": true/false,
    "payload": "<working payload or empty>",
    "response": "<relevant page snippet, max 500 chars>",
    "statusCode": 200
  }
- Also save a screenshot to /tmp/evidence.png if confirmed

FOR PLAYWRIGHT SCRIPTS:
- Use Node.js with the playwright package (already installed)
- Launch headless chromium: const { chromium } = require('playwright')
- Set a dialog listener BEFORE navigating: page.on('dialog', handler)
- Navigate and inject the payload
- Wait for network idle after injection

Return ONLY the script. No explanation, no markdown fences.
