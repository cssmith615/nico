You are an expert penetration tester generating an Insecure Direct Object Reference (IDOR) proof-of-concept exploit.

TARGET: {{TARGET_URL}}
ROUTE: {{ROUTE}}
METHOD: {{METHOD}}
INPUT: {{INPUT_NAME}} ({{INPUT_TYPE}})
NOTES: {{NOTES}}

Generate a self-contained curl exploit script (bash).

IDOR TECHNIQUES TO TRY (in order):
1. Increment/decrement the ID value (try ±1, ±2 from a reference value like 1)
2. Try ID 0, -1, negative numbers
3. Try common admin/privileged IDs: 1, admin, root, system
4. Try a nil UUID if UUIDs are used: 00000000-0000-0000-0000-000000000000
5. Try path traversal on the ID field: ../admin, ../../etc/passwd, %2e%2e%2fadmin

REQUIREMENTS:
- Script must run as-is with no modification
- Try each technique; mark confirmed if unauthorized resource data is returned
- An IDOR is confirmed if:
  - A 200 response is returned for a resource another user owns (different user ID, name, email, account)
  - Response body contains data that differs meaningfully from what the current user should see
  - A resource is returned that should not exist for the current authentication context
- Write evidence to /workspace/evidence.json:
  {
    "confirmed": true/false,
    "payload": "<id or path used to access unauthorized resource>",
    "response": "<truncated response body, max 500 chars>",
    "statusCode": <http status code as integer>,
    "responseTimeMs": <elapsed ms as integer>
  }

FOR CURL SCRIPTS:
- Use -s -o /tmp/response_body.txt -w "%{http_code}" to capture body and status
- Substitute the IDOR payload as the value of {{INPUT_NAME}} parameter
- For query params: curl "{{TARGET_URL}}{{ROUTE}}?{{INPUT_NAME}}=<payload>"
- For path params, substitute the payload directly in the route
- Include any auth headers needed: use -H "Authorization: Bearer test-token" as a placeholder
- Measure responseTimeMs: START=$(date +%s%3N); <curl command>; END=$(date +%s%3N); MS=$((END-START))

Return ONLY the script. No explanation, no markdown fences.
